<h2><%= @user.name %>さんのレビュー一覧</h2>
<table>
  <thead>
      <th>投稿日時</th>
      <th>評価球団</th>
      <th>試合日時</th>
      <th></th>
  </thead>
  <tbody>
    <% @reviews.each do |review| %>
    <tr>
      <td><%= review.created_at.strftime('%Y/%m/%d') %></td>
      <td><%= review.team.name %></td>
      <td><%= review.game_information.game_day %></td>
      <td><%= link_to 'レビュー詳細', admin_review_path(review), class:"btn btn-success" %></td>
    </tr>
    <% end %>
  </tbody>
</table>

<%= paginate @reviews %>